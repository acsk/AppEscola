Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Se a requisição já está dentro de /dist, não redireciona
  RewriteCond %{REQUEST_URI} ^/dist/ [NC]
  RewriteRule ^ - [L]

  # Arquivos físicos (js, css, png etc.) dentro de dist servidos diretamente
  RewriteCond %{DOCUMENT_ROOT}/dist%{REQUEST_URI} -f
  RewriteRule ^(.*)$ dist/$1 [L]

  # Assets do Expo (npm run web:build): sem fallback SPA — evita HTML no lugar de .js/.css
  RewriteCond %{REQUEST_URI} ^/_expo/ [NC]
  RewriteRule ^ - [L]

  # Rotas do app (hash) → dist/index.html (SPA)
  RewriteRule ^ dist/index.html [L]
</IfModule>
